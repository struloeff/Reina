{
  "name": "for-reina",
  "private": true,
  "version": "2.0.0",
  "description": "Source for the page one folder up. `npm run build` writes ../assets/show.js and ../assets/earth/textures.js.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@fontsource-variable/newsreader": "^5.3.0",
    "@fontsource/instrument-serif": "^5.3.0",
    "esbuild": "^0.28.2",
    "three": "^0.186.0"
  }
}
